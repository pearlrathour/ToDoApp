<%- include("header") -%>

    <!-- <% if(dayname==="Sunday" || dayname==="Thursday"){ %>
        <h1 style="color: dodgerblue;"><%= dayname %> List</h1>
    <%} else{ %>
        <h1 style="color: chartreuse;"><%= dayname %> List</h1>
    <%} %>    -->

    <div class="box" id="heading">
        <h1><%= dayname %></h1>
    </div>

    <div class="box">

        <% newtaskitem.forEach(function(task){ %>
            <form action="/delete" method="post">
                <div class="item">
                    <input type="checkbox" name="checkbox" value="<%= task._id%>" onChange="this.form.submit()">
                    <p><%= task.name %></p>
                </div>
            </form>
            <% }); %>

                <form class="" action="/" method="post">
                    <input type="text" name="newtask" placeholder="Enter New Task">
                    <button type="submit" name="submit">+</button>
                </form>
    </div>

    <%- include("footer") -%>